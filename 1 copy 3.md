# 工作规划时间板——页面交互设计需求文档

## 1. 文档目的

本文档用于描述“工作规划时间板”页面的**页面结构、交互方式、状态反馈及交互规则**，作为产品、UI、前端开发及测试实现页面时的统一依据。

本页面当前定位为：

> **帮助用户围绕母任务拆分子任务，并基于具体日期快速安排未来工作，同时表达母任务之间的需求依赖关系。**

现阶段重点解决“**计划什么时候做什么**”，而不是“项目当前执行得怎么样”。

因此本页面暂不承担传统项目进度管理中的完成率、延期、实际工时、风险、负责人负载、关键路径等能力。

---

# 2. 页面核心使用场景

典型使用流程为：

**查看所有母任务**
→ 展开某个母任务
→ 查看已有子任务
→ 新增子任务
→ 给子任务设置日期
→ 在时间板上拖动调整时间
→ 设置母任务间的依赖关系
→ 根据依赖关系重新调整整体规划。

用户应尽可能在当前时间板内完成规划，而不需要频繁跳转其他页面。

---

# 3. 页面整体结构

页面从上到下分为三个主要区域。

### 3.1 时间控制工具栏

主要负责：

* 前后切换时间范围
* 回到今天
* 切换周 / 双周 / 月视图
* 查看当前展示的日期范围
* 开启 / 关闭需求依赖关系展示

基本结构：

```text
←    今天    →

2026/09/14 – 2026/09/27

[ 周 ][ 双周 ][ 月 ]

☑ 显示依赖关系
```

---

### 3.2 时间刻度区域

左侧固定显示：

> 母任务 / 子任务

右侧展示具体日期。

例如双周模式：

```text
9/14
周一

9/15
周二

9/16
周三
……
9/27
周日
```

横轴时间单位始终基于**具体日期**，不再使用：

> W1 / W2 / W3

这样的周编号作为主要刻度。

---

### 3.3 工作规划画板

左侧：

```text
母任务
    子任务
    子任务
母任务
    子任务
```

右侧：

```text
日期时间线 + 子任务时间条 + 母任务总体时间跨度 + 依赖关系线
```

左右必须严格保持行对应关系。

---

# 4. 时间范围交互

## 4.1 周 / 双周 / 月切换

页面提供三种显示跨度：

| 模式 |  展示范围 | 日期粒度 |
| -- | ----: | ---- |
| 周  |    7天 | 天    |
| 双周 |   14天 | 天    |
| 月  | 当前自然月 | 天    |

默认推荐：

> **双周**

原因是工作规划时，一周往往过短，一个月又容易造成单日宽度过窄，双周更适合日常工作安排。

---

## 4.2 切换视图

点击：

> 周 / 双周 / 月

仅改变：

* 当前可视时间跨度
* 每一天在横轴上的显示宽度

不得改变：

* 子任务数据
* 起止日期
* 母任务关系
* 依赖关系

例如：

```text
双周：
9/14 ---------------------- 9/27

切换月：

9/1 ------------------------------------------ 9/30
```

已有子任务根据其真实日期重新映射到时间线上。

---

# 5. 时间导航

## 5.1 上一周期

点击：

> ←

根据当前模式移动：

* 周：向前7天
* 双周：向前14天
* 月：进入上一个自然月

---

## 5.2 下一周期

点击：

> →

规则同上，方向相反。

---

## 5.3 回到今天

点击：

> 今天

时间范围重新定位到包含今天的周期。

例如今天为：

> 2026/09/17

双周模式则回到包含 9/17 的双周时间段。

---

# 6. 日期显示规则

日期至少显示：

> 月 / 日
> 星期

例如：

```text
9/17
周四
```

今天需要有明显但不过度突出的视觉提示。

建议采用：

* 日期格浅色高亮
* 一条垂直“今天线”

用于帮助用户快速定位当前时间。

---

# 7. 周末显示

周六、周日建议使用轻微不同的背景色。

目的不是禁止排任务，而只是帮助用户快速识别：

> 工作日 / 周末

用户仍然可以：

* 在周末新建子任务
* 将子任务拖到周末
* 设置周末作为起止日期

---

# 8. 母任务展示

每个母任务占据一行。

基本结构：

```text
⌄ Slots第一期策略          依赖 1   依赖   +子任务
```

母任务行主要包含：

* 展开 / 收起
* 母任务名称
* 依赖数量
* 依赖异常提示
* 设置依赖
* 添加子任务

---

# 9. 展开 / 收起母任务

点击母任务前面的：

> `⌄` / `>`

控制其子任务展开状态。

### 展开

显示：

```text
⌄ Slots第一期策略
      体验模板设计
      策略开发
      测试验证
```

### 收起

只显示：

```text
> Slots第一期策略
```

但即使收起，右侧仍需保留：

> **母任务整体排期条**

---

# 10. 母任务整体排期

母任务自身不单独配置时间。

其时间范围由所有子任务自动计算。

规则：

```text
母任务开始日期
=
最早子任务开始日期

母任务结束日期
=
最晚子任务结束日期
```

例如：

```text
子任务A：9/17—9/20
子任务B：9/21—9/22
子任务C：9/25
```

则母任务整体排期：

> 9/17—9/25

画板上使用较细、较弱的时间条表示。

这样用户即使收起母任务，也能快速判断整体工作分布。

---

# 11. 子任务展示

子任务展开后：

```text
    体验模板设计
    策略开发
    测试验证
```

右侧对应显示任务条。

例如：

```text
             9/17  9/18  9/19  9/20

体验模板设计  ━━━━━━━━━━━━━
```

子任务条宽度代表持续时间。

---

# 12. 单日任务

如果：

```text
开始日期：9/17
结束日期：9/17
```

则显示为单个日期宽度的任务条。

如果用户只填写：

> 开始日期：9/17

没有填写结束日期，则系统自动按照：

> 开始日期 = 结束日期

处理。

即：

> **默认工期1天。**

---

# 13. 从母任务新增子任务

母任务右侧提供明确入口：

> `+ 子任务`

点击后打开“新增子任务”设置框。

---

# 14. 新增子任务弹窗

弹窗暂时只包含必要信息：

```text
新增子任务

子任务名称
[________________]

开始日期
[ 2026/09/17 ]

结束日期（可不填）
[              ]

结束日期不填时默认1天

取消                确定
```

---

# 15. 新增子任务字段规则

### 子任务名称

必填。

为空时点击确定：

> 请输入子任务名称

不关闭弹窗。

---

### 开始日期

必填。

---

### 结束日期

非必填。

如果为空：

```text
结束日期 = 开始日期
```

---

### 日期合法性

必须：

```text
结束日期 >= 开始日期
```

如果：

```text
开始：9/20
结束：9/18
```

提示：

> 结束日期不能早于开始日期

---

# 16. 从时间板直接新增子任务

除了：

> `+ 子任务`

之外，还应支持更适合规划场景的快速方式。

用户在某个母任务右侧时间区域：

> **双击具体日期**

例如在：

> Slots第二期策略 × 9/24

位置双击。

系统打开新增子任务弹窗，并自动设置：

```text
开始日期 = 2026/09/24
结束日期 = 空
```

用户只需要填写：

> 子任务名称

即可完成新增。

---

# 17. 快速新增后的状态

新增完成后：

1. 自动保存
2. 如果母任务原本处于收起状态，可自动展开
3. 新任务立即显示在对应日期
4. 母任务总体时间范围重新计算

例如：

原来：

> 母任务：9/17—9/22

新增：

> 数据验证：9/25

则母任务自动变为：

> 9/17—9/25

---

# 18. 点击已有子任务

点击子任务时间条：

> 打开“设置子任务时间”弹窗。

现阶段不进入复杂任务详情。

弹窗：

```text
设置子任务时间

子任务
策略开发

开始日期
[2026/09/21]

结束日期
[2026/09/22]

取消                确定
```

---

# 19. 修改子任务日期

修改：

> 开始日期 / 结束日期

点击确定后：

* 更新任务条位置
* 更新任务条宽度
* 更新母任务整体时间
* 重新检查需求依赖冲突

不要求用户刷新页面。

---

# 20. 清空结束日期

编辑已有任务时，如果用户清空结束日期：

例如：

```text
开始：9/23
结束：
```

系统解释为：

> 9/23 单日任务

而不是：

> 未完成时间设置

---

# 21. 子任务拖动改期

用户可以直接拖动子任务时间条。

例如当前：

```text
9/21 — 9/23
```

整体向右拖2天：

```text
9/23 — 9/25
```

---

# 22. 拖动规则

拖动改变：

> 开始日期 + 结束日期

但不改变：

> 原持续时间。

例如：

```text
原计划：
9/21—9/23
持续3天
```

拖到9/25：

```text
新计划：
9/25—9/27
持续仍为3天
```

---

# 23. 日期吸附

拖动不得采用纯像素定位。

拖动后必须：

> **自动吸附到完整日期单位。**

不能产生：

> 9/23 14:00

类似结果。

本页面时间精度目前固定为：

> 天。

---

# 24. 点击与拖动区分

子任务同时支持：

* 点击
* 拖动

因此需要设置判断阈值。

建议：

小幅移动：

> 视为点击

明显横向移动：

> 视为拖动

防止用户想打开时间设置时误修改计划。

---

# 25. 拖动反馈

拖动过程中建议高亮任务条。

释放后提示：

> 已将“策略开发”调整为 9/23 – 9/25

提示采用：

> Toast

短暂出现后自动消失。

不需要额外确认弹窗。

---

# 26. 跨当前时间范围任务

任务可以超出当前视图区间。

例如：

当前查看：

> 9/14—9/27

任务：

> 9/25—10/03

则当前画板只显示：

> 9/25—9/27

对应部分。

任务真实数据仍然保持：

> 9/25—10/03

切换下一周期即可看到剩余部分。

---

# 27. 母任务需求依赖

支持在：

> **母任务之间**

设置需求依赖。

当前不支持：

* 子任务 → 子任务
* 子任务 → 母任务
* 母任务 → 子任务

---

# 28. 依赖关系定义

统一采用：

> 当前母任务依赖某个前置母任务

例如：

```text
Slots第二期策略
依赖
Slots第一期策略
```

逻辑表示：

```text
Slots第一期策略
        ↓
Slots第二期策略
```

---

# 29. 设置依赖入口

母任务行提供：

> `依赖`

按钮。

点击打开：

> 设置需求依赖弹窗。

---

# 30. 需求依赖弹窗

例如：

```text
设置需求依赖

当前母任务
Slots第二期策略

前置母任务

☐ 埋点开发及接入
☐ Dummy机器人梯度
☐ Slots现状分析
☑ Slots第一期策略
☐ 日常/周任务系统

取消              保存依赖
```

---

# 31. 多重依赖

一个母任务允许配置：

> 多个前置母任务。

例如：

```text
VIP系统

依赖：
日常/周任务系统
埋点系统
支付系统
```

保存后显示：

> 依赖 3

---

# 32. 当前母任务不可选择自身

配置：

> Slots第二期策略

时，候选列表中不显示：

> Slots第二期策略

防止自我依赖。

---

# 33. 循环依赖校验

系统必须禁止形成循环依赖。

例如已有：

```text
A → B
B → C
```

则用户不能再配置：

```text
C → A
```

保存时提示：

> 该设置会形成循环依赖，请调整前置母任务。

并阻止保存。

---

# 34. 依赖数量展示

存在依赖时，在母任务名称附近展示：

> `依赖 2`

用于快速识别该母任务存在多少个前置需求。

没有依赖的母任务无需显示：

> 依赖 0

避免视觉噪音。

---

# 35. 时间线依赖关系线

开启：

> ☑ 显示依赖关系

后，在母任务整体排期条之间绘制：

> 依赖箭头。

例如：

```text
Slots现状分析
━━━━━━
      ↘

Slots第一期策略
      ━━━━━━━
             ↘

Slots第二期策略
             ━━━━━━━
```

箭头方向：

> 前置母任务 → 后置母任务。

---

# 36. 显示 / 隐藏依赖线

顶部提供：

> 显示依赖关系

开关。

关闭后：

* 隐藏所有依赖线
* 不删除依赖数据
* `依赖 N` 标记仍可以保留

适用于母任务较多、依赖线较复杂的情况。

---

# 37. 依赖与计划日期冲突

依赖关系本质上是：

> 规划提示

而不是强制排程。

例如：

```text
A：9/15—9/20

B：9/18—9/25

B依赖A
```

存在时间重叠。

此时：

> B 显示依赖冲突提示。

例如：

> ⚠

---

# 38. 冲突判断规则

当前可采用简单规则：

如果：

```text
后置母任务开始日期
<=
前置母任务结束日期
```

则认为：

> 当前计划存在潜在依赖冲突。

例如：

```text
A结束：9/20
B开始：9/20
```

也提示冲突。

如果：

```text
A结束：9/20
B开始：9/21
```

则正常。

---

# 39. 冲突视觉反馈

正常依赖：

> 普通依赖箭头

冲突依赖：

> 使用警示样式，如虚线 / 警示色。

母任务行同时出现：

> ⚠

让用户即使不仔细查看连线，也能发现问题。

---

# 40. 依赖关系不自动修改排期

这是当前交互设计中的重要原则。

发现：

> B 与前置A时间重叠

系统只：

> 提示冲突。

不得：

> 自动把 B 移到 A 后面。

用户可以自行判断：

* 是否允许部分并行
* 是否需要调整
* 如何调整最合理

从而保持规划自由度。

---

# 41. 拖动后的依赖检查

用户拖动子任务后：

1. 子任务日期发生变化
2. 母任务时间范围重新计算
3. 系统重新检查母任务依赖
4. 更新依赖线
5. 更新冲突标识

整个过程自动完成。

---

# 42. 修改日期后的依赖检查

通过日期设置弹窗修改子任务，与拖动规则一致：

> 每次修改计划日期后，都重新计算母任务依赖关系。

---

# 43. 画板横向滚动

时间跨度大于当前页面宽度时：

> 时间区域允许横向滚动。

左侧：

> 母任务 / 子任务名称

保持固定。

避免滚动到后续日期时不知道当前查看的是哪个任务。

---

# 44. 页面纵向浏览

母任务和子任务较多时，页面自然纵向延展。

左侧任务区与右侧时间区必须：

> 保持统一行高。

否则时间条会与任务名称错位。

---

# 45. Hover反馈

桌面端鼠标移入子任务时间条时：

* 增强边框或背景
* Cursor 显示可拖动状态
* Tooltip 可显示：

```text
策略开发
9/21—9/22

可拖动调整时间
点击设置日期
```

但所有核心功能不能仅依赖 Hover。

---

# 46. 自动保存原则

本页面采用：

> 操作即保存

以下操作完成后直接保存：

* 新增子任务
* 修改子任务日期
* 拖动子任务
* 设置需求依赖
* 删除需求依赖

页面无需再提供：

> “保存整个规划”

按钮。

---

# 47. 页面反馈原则

不同操作采用不同反馈强度。

轻量操作：

> Toast

例如：

> 已调整至 9/23–9/25

需要阻止用户继续操作的错误：

> 弹窗内直接显示错误信息

例如：

> 结束日期不能早于开始日期。

需要用户关注但不阻止工作的情况：

> 页面警示

例如：

> ⚠ 当前计划与前置需求存在时间冲突。

---

# 48. 当前版本不包含的功能

为了保持“工作规划时间板”的定位，本阶段明确暂不进入以下功能：

* 子任务完成状态
* 母任务完成状态
* 完成百分比
* 实际完成时间
* 实际开始时间
* 延期判断
* 逾期标红
* 风险管理
* 工时统计
* 人员负载
* 子任务依赖
* 关键路径
* 自动排程
* 自动推迟后续需求
* 资源冲突自动解决

这些属于：

> **项目执行与进度管理**

而当前页面主要服务于：

> **工作计划编排。**

---

# 49. 页面核心交互关系总结

整个页面最核心的操作模型可以概括为：

```text
                         ┌─ 点击 → 修改日期
                         │
子任务时间条 ────────────┼─ 拖动 → 整体调整日期
                         │
                         └─ 日期变化 → 母任务排期自动变化
                                          │
                                          ↓
                                  重新检查依赖关系


母任务 ── +子任务 ──────→ 新增子任务

母任务时间区域 ── 双击日期 → 快速新增子任务

母任务 ── 设置依赖 ─────→ 选择前置母任务
                                  │
                                  ↓
                         形成母任务依赖连线
                                  │
                     ┌────────────┴───────────┐
                     ↓                        ↓
                  正常依赖                 时间冲突
                     ↓                        ↓
                 普通连线               ⚠警示连线
```

---

# 50. 当前版本核心验收条件

开发完成后，至少应保证以下完整操作链可以正常完成：

1. 用户可以切换**周 / 双周 / 月**。
2. 横轴显示真实具体日期。
3. 用户可以前后切换时间周期并回到今天。
4. 用户可以展开 / 收起母任务。
5. 母任务可以正确汇总全部子任务的时间范围。
6. 用户可以通过 `+子任务` 添加子任务。
7. 用户可以直接双击时间轴日期快速添加子任务。
8. 只填写开始日期时，任务默认持续一天。
9. 用户点击子任务可以修改开始 / 结束日期。
10. 用户可以直接拖动子任务改变整体时间。
11. 拖动后任务持续天数保持不变。
12. 日期变化后母任务时间范围自动重算。
13. 用户可以设置一个或多个母任务作为前置依赖。
14. 系统禁止自我依赖和循环依赖。
15. 依赖关系可以通过时间线箭头显示。
16. 用户可以关闭依赖关系线。
17. 排期与依赖关系冲突时能够显示警示。
18. 系统不会因为依赖冲突自动修改计划日期。
19. 子任务跨当前显示周期时可以正确截取可见部分。
20. 左侧任务和右侧时间线始终保持正确对应。

这份文档即可作为当前原型进入 UI 细化和前端实现时的**页面交互设计基线**。
