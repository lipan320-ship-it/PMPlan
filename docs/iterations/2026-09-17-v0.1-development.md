# 迭代：v0.1.0 开发

- 日期：2026-09-17
- 状态：In Progress
- 当前里程碑：M2 领域规则与 SQLite 数据内核
- 关联需求：[工作规划时间板需求基线](../requirements/planning-board-prd.md)
- 实施计划：[v0.1 开发实施计划](../development/v0.1-implementation-plan.md)
- 关联决策：[ADR-0001](../decisions/ADR-0001-local-first-tauri-desktop.md)、[ADR-0002](../decisions/ADR-0002-rust-sqlite-storage-boundary.md)

## 目标

按已接受的 M0–M7 计划交付 Windows 优先、可离线使用的工作规划时间板 v0.1.0。

## 里程碑状态

| 里程碑 | 状态 | 结果 |
| --- | --- | --- |
| M0 范围落档 | Completed | 计划获批；补齐最小任务管理需求；接受 Rust SQLite 存储边界 |
| M1 工程与桌面壳 | Completed | 前后端工程、离线资源、测试与桌面 debug 构建已验证 |
| M2 领域与数据内核 | In Progress | 下一步建立领域类型、SQLite migration 与业务 commands |
| M3 可用时间板 | Not Started | — |
| M4 直接操作 | Not Started | — |
| M5 依赖关系 | Not Started | — |
| M6 数据交换 | Not Started | — |
| M7 发布候选 | Not Started | — |

## M0 完成结果

- 实施计划由 Proposed 更新为 Accepted。
- 需求基线增加母任务和子任务的最小新建、重命名与删除规则。
- 明确首次启动为空白画板，不写入演示数据。
- 新增 ADR-0002，确认 Rust 存储层、bundled SQLite、业务级 commands 和事务边界。

## M1 完成结果

- 初始化 React 19、TypeScript、Vite、Tailwind CSS 和 Tauri 2 单包工程。
- 建立最小 capability；未开放 shell、通用 SQL、任意文件路径或网络能力。
- 字体使用系统字体，应用图标由仓库内 SVG 生成，运行资源不依赖 CDN。
- 建立 TypeScript、ESLint、Vitest、Markdown、Rust fmt/clippy/test 与桌面构建命令。
- 安装并验证 Rust MSVC、rustfmt、clippy 与 Visual Studio C++ Build Tools。
- npm 审计为 0 个已知漏洞。

## M1 验证结果

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm run lint` | 通过 |
| `npm test` | 1 个测试通过 |
| `npm run lint:md` | 15 个 Markdown 文件通过 |
| `npm run build` | 通过 |
| Vite 开发服务器冒烟 | HTTP 200，测试后已关闭进程 |
| `cargo fmt --check` | 通过 |
| `cargo test` | 1 个 Rust 测试通过 |
| `cargo clippy --all-targets --all-features -- -D warnings` | 通过 |
| `tauri build --debug --no-bundle` | 通过，生成 `pmplan.exe` |
| EXE 启动冒烟 | 运行 3 秒未退出，网络连接数为 0 |
| `npm audit` | 0 个已知漏洞 |

说明：M1 验证的是工程壳与离线资源，不代表 M2–M7 产品功能已经完成。

## 后续记录

每完成一个里程碑，在本文件补充交付物、验证命令、实际结果和遗留事项，再进入下一里程碑。
